{
  "name": "dsh-auto-diff-v1",
  "version": "1.0.0",
  "private": true,
  "description": "Auto-diff session snapshot plugin v1: captures files before every write/edit tool call and writes the unified diff next to the session's own record (auto-diff.patch beside session.jsonl.zstd under $DSH_HOME/sessions) at each session turn end.",
  "main": "index.cjs"
}
